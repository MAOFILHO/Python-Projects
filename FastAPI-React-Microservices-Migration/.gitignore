# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.eggs/
build/
dist/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.venv/
venv/
env/
*.egg

# SQLite / data (root data/ is kept — it holds the committed architecture diagram)
*.db
*.sqlite
*.sqlite3
services/*/data/

# Env files
.env
.env.*
!.env.example

# Local state left by azure/scripts/deploy.sh (whatever names it actually
# used, e.g. after an auto-incremented project name) — machine-local, not
# secret, not meant to be committed.
azure/.deploy-state

# Node (frontend, built by others but keep repo clean)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
dist/
.next/

# OS / editor
.DS_Store
.idea/
.vscode/
*.swp
